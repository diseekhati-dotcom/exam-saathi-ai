# Exam Saathi AI

Telegram exam study bot for Render Web Service.

Required Render environment variables:
- BOT_TOKEN = Telegram BotFather token
- WEBHOOK_URL = the public Render service URL, e.g. https://exam-saathi-ai.onrender.com

The app binds to 0.0.0.0:$PORT and uses Telegram webhook mode.
